#!/bin/bash

# Install some version of python
pyenv install 3.9.1
pyenv global 3.9.1

# .zshrc configuration already updated
# echo -e 'if command -v pyenv 1>/dev/null 2>&1; then\n  eval "$(pyenv init -)"\nfi' >> ~/.zshrc

pip3 install virtualenv # virtual environment
pip install --upgrade pip # upgrade to the newest version
